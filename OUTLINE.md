Portico is a local-first bookmark and startpage server.

It consists of a Go backend, a Typescript Solidjs SPA, and a web extension.

The web extension sends bookmarks to the backend, which stores them in a sqlite db.

The startpage displays all the bookmarks in a convenient grid.